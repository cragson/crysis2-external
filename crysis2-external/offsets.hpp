#pragma once

#include <cstdint>
#include <cstddef>

namespace Offsets
{
	inline auto infinite_ammo = std::uintptr_t();

	inline auto infinite_energy = std::uintptr_t();

	inline auto ai_ignore = std::uintptr_t();

	inline auto norecoil_crosshair = std::uintptr_t();

	inline auto bypass_conrestriction = std::uintptr_t();

	inline auto cgame = std::uintptr_t();

	inline auto infinite_energy2 = std::uintptr_t();
}